<div class="row">
    {{#each contacts}}
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title d-flex justify-content-between align-items-ccenter">
                        {{name}} <a href="/contacts/edit/{{_id}}"><i class="fa fa-pencil"></i></a>
                    </h4>
                    <p>{{lastname}}</p>
                    <form action="/contacts/delete/{{_id}}" method="POST">
                        <button type="submit" class="btn btn-danger btn-block btn-sm">
                            Delete
                        </button>
                    </form>
                </div>
            </div>
        </div>
    {{/each}}
</div>